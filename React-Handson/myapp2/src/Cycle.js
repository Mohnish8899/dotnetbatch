import React from 'react';

const Cycle = ({brand,model}) => {
    return (
      <div>
        <h1>
          My cycle brand is {brand} and the model is {model}
        </h1>
      </div>
    );
}
export default Cycle;
